[project]
name = "workspace"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "django-environ>=0.14.0",
    "pillow>=12.2.0",
]

[dependency-groups]
dev = [
    "djlint>=1.39.2",
]
