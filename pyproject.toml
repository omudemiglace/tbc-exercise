[project]
name = "workspace"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "django-two-factor-auth>=1.18.1",
]

[dependency-groups]
dev = [
    "djlint>=1.39.2",
]
